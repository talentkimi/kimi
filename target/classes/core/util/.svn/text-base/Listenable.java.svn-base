package core.util;

/** 
 * Implement this interface to create a concrete listenable class. This 
 * will allow Listeners to register/deregister from it.
 */
public interface Listenable 
{
	/** 
	 * Used by a Listener to register with the implementing class. 
	 * @param p_listener The registering Listener class. 
	 */
	public void register(Listener p_listener);
	
	/** 
	 * Used by a Listener to deregister from the implementing class. 
	 * @param p_name The Listener class deregistering from this object.
	 */
	public void deregister(Listener p_listener);
}
